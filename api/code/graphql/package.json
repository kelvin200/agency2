{
  "name": "api-graphql",
  "version": "0.1.0",
  "scripts": {
    "build": "yarn webiny run build",
    "watch": "yarn webiny run watch",
    "test": "jest --config jest.config.js --passWithNoTests --maxWorkers=1",
    "test:watch": "jest --watch --config jest.config.js --passWithNoTests --maxWorkers=1"
  },
  "devDependencies": {
    "@webiny/cli": "5.20.0"
  }
}
